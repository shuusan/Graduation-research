CREATE TABLE event_color (
id MEDIUMINT UNSIGNED AUTO_INCREMENT,
studentID SMALLINT UNSIGNED,
bottom_eventID SMALLINT UNSIGNED,
color VARCHAR(8),
PRIMARY KEY (id),
FOREIGN KEY (bottom_eventID)
REFERENCES bottom_event(id)
ON DELETE CASCADE,
INDEX (studentID)
)ENGINE=INNODB  DEFAULT CHARSET=utf8;