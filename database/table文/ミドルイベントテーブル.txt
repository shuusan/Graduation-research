CREATE TABLE middle_event (
id TINYINT AUTO_INCREMENT UNSIGNED,
top_eventID TINYINT UNSIGNED,
event_title VARCHAR(63),
PRIMARY KEY (id),
FOREIGN KEY (top_eventID)
REFERENCES top_event(id)
ON DELETE CASCADE,
INDEX (id)
)ENGINE=INNODB;