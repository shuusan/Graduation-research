CREATE TABLE preparation_event (
id SMALLINT UNSIGNED AUTO_INCREMENT,
top_eventID TINYINT UNSIGNED,
event_content VARCHAR(63),
timeNo INT UNSIGNED,
PRIMARY KEY (id),
FOREIGN KEY (top_eventID)
REFERENCES top_event(id)
ON DELETE CASCADE,
INDEX (id)
)ENGINE=INNODB;