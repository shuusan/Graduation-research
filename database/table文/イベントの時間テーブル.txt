CREATE TABLE time (
id SMALLINT UNSIGNED AUTO_INCREMENT,
bottom_eventID TINYINT UNSIGNED,
datetime DATETIME,
PRIMARY KEY (id),
FOREIGN KEY (bottom_eventID)
REFERENCES middle_event(id)
ON DELETE CASCADE,
INDEX (id)
)ENGINE=INNODB;