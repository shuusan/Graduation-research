CREATE TABLE preparation_time (
id SMALLINT UNSIGNED AUTO_INCREMENT,
top_eventID TINYINT UNSIGNED,
datetime DATETIME,
PRIMARY KEY (id),
FOREIGNKEY (top_eventID)
REFERENCES preparation_event(id)
ON DELETE CASCADE,
INDEX(id)
)ENGINE=INNODB;